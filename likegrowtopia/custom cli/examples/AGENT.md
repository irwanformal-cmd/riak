# AGENT.md — project instructions

This file is injected into every agent run as project context (AGENT.md).

## Project
A provider-agnostic cloud agent CLI + plugin platform.

## Rules
- Prefer boring, maintainable architecture.
- Keep providers, plugins, tools, and UI decoupled.
- Never hard-code API keys or log secrets.
- Write tests alongside implementation.
